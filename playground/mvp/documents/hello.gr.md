---
version: "1.0"
name: hello-document
inputSchema:
  type: object
  properties:
    name:
      type: string
  required:
    - name
state:
  greeting: null
  items: null
steps:
  - id: build-greeting
    kind: assign
    set:
      greeting: ${"Hello " + input.name + "!"}
      items:
        - first
        - second
        - third
output:
  greeting: ${state.greeting}
  items: ${state.items}
  name: ${input.name}
---

# ${output.greeting}

Welcome to Glyphrail Documents, ${output.name}!

## Items

${output.items | bullets}

## Details

- Greeting: ${output.greeting}
- Name: ${output.name | upper}

---
*Generated by Glyphrail*
