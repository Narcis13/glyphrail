version: "1.0"
name: __WORKFLOW_NAME__
description: Basic workflow scaffold generated by glyphrail

state:
  result: null

steps:
  - id: start
    kind: assign
    set:
      result: "ok"

  - id: done
    kind: return
    output:
      result: ${state.result}
